version:"3.9"
services:
  server:
    build: /server
    ports:
      - "3000:3000"
    restart: always

  client:
    build: /client
    ports:
      - "5173:5173"
    depends_on:
      - server
    restart: always